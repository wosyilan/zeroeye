[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short --no-header -q
markers =
    offline: tests that run without network (default).
    live: tests that require a running backend.
